[pytest]
# La racine du projet doit être sur le sys.path pour « import app.backend… ».
pythonpath = .
testpaths = tests
# Les tests de l'agent de traduction sont asynchrones : pytest-asyncio les prend en charge
# automatiquement, sans marqueur explicite sur chaque fonction.
asyncio_mode = auto
